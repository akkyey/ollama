# ローカルLLM エージェント運用に関する知見まとめ

このドキュメントは、Kilo Code（またはRoo Code）などの自律型AIエージェントをローカルLLM（llama.cpp）で運用する際に得られた、重要な知見とベストプラクティスをまとめたものです。

## 1. ツール呼び出し精度における「量子化」と「モデルの性格」の関係

自律型エージェントを稼働させる上で、最も重要なのが**「AIがシステム側の要求するツールフォーマット（JSONやXMLタグ）を厳密に守れるか」**という点です。
当初は「3-bitの過度な量子化」が原因だと推測されていましたが、対照実験により以下の結論に至りました。

* **モデル固有のフォーマット遵守能力（最重要）**:
  * ❌ `Qwen2.5-Coder-32B-Instruct`
    * 3-bit（IQ3_M）でも 4-bit（Q4_K_M）でも、Kilo Codeのツール呼び出しフォーマット（XMLタグ等）を無視してパースエラー（`MODEL_NO_TOOLS_USED`）を連発します。コーディングには優れていますが、自律エージェントとしては不適格です。
  * ⭕️ `Qwen3.6-27B` (Architect用) / `Qwen2.5-14B` (Fast用)
    * こちらは4-bit（Q4_K_M）で稼働させた際、完璧にツールタグを出力して自律動作を完遂しました。

**【結論】**
エージェントの安定稼働には、量子化率よりも**「モデルが本来持っている、XMLフォーマット等の指示を厳密に守る性格（Instruction Following能力）」**が決定的に重要です。現状のKilo Code運用においては、Coderモデルではなく**「Qwen 3.6 27B (Architectモデル)」**を主軸に設計・実装を全て任せる運用が最も安定します。

## 2. タスク（チャット）の分割によるコンテキストの最適化

ローカルLLMは、コンテキスト（過去の会話履歴）が長くなるほど、プロンプトの読み込み（Prompt Evaluation）に膨大な時間がかかります。

* **ベストプラクティス**:
  * **設計と実装の分離**: Architectモードで要件定義や設計書（Markdown）を作成させた後、**一度チャットを終了（新規チャットを作成）**します。
  * 次のチャットで、完成した設計書だけを読み込ませて実装を開始させます。これにより、設計段階の不要なやり取り（数万トークン）がリセットされ、AIの処理時間が劇的に短縮（例: 30分→数分）されるだけでなく、ハルシネーション（バグ）の発生率も低下します。

## 3. Agentic Switch（プロキシ）による動的モデル切り替え

用途に応じて最適なモデルに切り替えるため、間にプロキシサーバー（例: `switch_llama.py`）を挟むアーキテクチャは非常に有効です。

* **仕組み**: Kilo Codeからのリクエストヘッダーやパラメータ（slug）をプロキシが傍受し、内部の `llama-server` を動的に再起動してモデルを切り替えます。
* **注意点**: モデル切り替えが発生すると、LLMのキャッシュがクリアされるため、直後のリクエストは初回読み込みのオーバーヘッドが発生します。

## 4. エラー時の手動リカバリ

AIが長時間の推論の末にフォーマットエラー（`MODEL_NO_TOOLS_USED`）を起こした場合、単純なRetryを繰り返すと、再び同じ推論時間（数十分）を待たされることになります。

* **対策**:
  * AIがチャット画面上に生のJSONやコードを出力している場合は、**Kilo Code上の処理を直ちにキャンセル**し、人間が手動でファイルを作成・コピペして完了させます。
  * ツールフォーマットを再認識させたい場合は、「必ず `<tool_call>` と `</tool_call>` のタグでJSONを囲んでください」といった**直接的なフォーマット指定のプロンプトを投げてからRetry**させます。

## 5. モデル別 実験結果と実行時間サマリー（2026/05/28 時点）

Hacker Newsスクレイパーの実装タスクにおける、各モード（モデル）の検証結果と実行時間のまとめです。

* ❌ **Codeモード (Qwen2.5-Coder-32B-Instruct)**
  * **結果**: `NG` (MODEL_NO_TOOLS_USED エラー)
  * **所要時間**: 計測不能（パースエラーで中断）
  * **原因**: 3-bit (IQ3_M) および 4-bit (Q4_K_M) 共に、ツールフォーマット（XML）の出力指示を無視する傾向が強いため。

* ❌ **Askモード (Qwen2.5-14B-Instruct-Q4_K_M)**
  * **結果**: `NG` (要件忘却・暴走)
  * **所要時間**: 約 35 分
  * **原因**: Kilo Code側のデフォルト設定（fast）として起動。初期はツールを呼び出せたものの、コンテキストが肥大化した結果、要件を忘れて異常な長文を出力し続けるループに陥った。

* ⭕️ **Architectモード (Qwen3.6-27B-Q4_K_M)**
  * **結果**: `OK` (設計〜実装〜ファイル保存まで完全成功)
  * **所要時間**: 約 2 時間 2 分
    * 設計・要件定義フェーズ: 約 1 時間 1 分（約1,500トークン）
    * コード実装フェーズ: 約 44 分
    * 事後処理フェーズ: 約 17 分
  * **特記事項**: 巨大なマークダウン設計書を出力した直後でも、ツール呼び出しフォーマットを完璧に維持。21スレッドでのCPU合計稼働時間は「5時間22分」に及んだが、一切の破綻なく自律完遂した。

* ⏳ **Reviewモード (Gemma-4-31B-it-Q3_K_M)**
  * **結果**: 未検証（検証予定）

## 6. プロキシ最適化と完全自律稼働ベンチマーク（2026/05/28 追加）

プロキシサーバー（`kilo_proxy.py`）から、不要な「プロンプト削減機能（`truncate_workspace`）」と「動的モデル切り替え機能」を排除した結果、**一切のパースエラーなく、APUによる完全な自律稼働ループが成立**することが証明されました。

* **使用モデル**: Qwen3.6-27B-Q4_K_M
* **タスク**: 設計書（Markdown）からのHacker Newsスクレイパー（Python）の実装とテスト実行
* **総合所要時間**: 約 1時間

**【各フェーズの実行時間と知見】**

1. **初期コンテキスト読み込み（約17分）**
   * 入力プロンプト約13,000トークンの初回読み込み（Prompt Evaluation）。
2. **要件理解と準備（約5分）**
   * `.kilo` や `scraper_plan.md` の読み込み、および `requirements.txt` の作成。
3. **超大規模コーディング（約30分）**
   * `main.py` の実装。APUによる秒間約1.35トークンの生成速度で、**一回の出力として2,452トークン（221行）**のPythonコードをノンストップで生成完了。
   * プロキシがストリーミングを束ねて送信したため、フォーマット崩れは一切発生せず。
4. **コンテキスト再評価（約3.5分）**
   * 直前に生成した巨大コード（約2,400トークン）を自身の記憶（コンテキスト）に再読み込み。キャッシュヒット率87.5%を記録し、秒間12トークンの速度で高速処理。
5. **エラー自己修復とテスト実行（数分）**
   * `pip install requests` 実行時にLinux特有の **PEP 668（グローバル環境インストールエラー）** に直面するも、AIが自律的にエラーログを解釈。
   * 人間の介入なしに `--break-system-packages` フラグを付与して再実行するという、極めて高度な自己修復能力を披露。スクリプトを完走させ、CSVとHTMLを正常に出力。

**【結論】**
APUサーバー（GPUなし）であっても、コンテキストキャッシュを適切に管理し、プロキシでフォーマットエラーを防御すれば、**「2万トークン超えの記憶を維持しつつ、数千トークン規模のコードを一気に書き上げ、環境エラーをも自己解決する最強のローカルエージェント」**が構築可能です。

## 7. 長時間稼働と超大容量コンテキスト（27Kトークン）のストレステスト（2026/05/28 追加）

「お題3（psutilを使ったASCIIアートのリソース監視ツール）」にて、End-to-End（要件定義〜設計〜実装〜テスト）の完全自律稼働を検証しました。

* **使用モデル**: Qwen3.6-27B-Q4_K_M (Architectモード)
* **タスク**: PCのリソースを監視し、直近1分間の推移をターミナル上にASCIIアートでリアルタイム更新（--watch）する CLI ツールの作成。
* **総所要時間**: 約 5時間17分（18:30〜23:47 実働）
  * **特記事項**: 実行中にコンテキストサイズが **27,000トークン** を突破。

**【各フェーズの実行時間と知見】**

1. **設計・アーキテクチャ定義**
   * 非常に精巧な設計書（`design_doc.md`）を出力。データ収集と描画スレッドの分離（`threading.Lock` の使用）、ブロック文字（`█▓▒░`）による密度表現、Windows等の非UTF-8環境へのフォールバックなど、プロフェッショナルレベルのアーキテクチャを立案。
2. **実装・自己修復・デバッグの持久戦**
   * 実装フェーズに入った後も、巨大なコンテキスト（過去の会話やコードの文脈）を失うことなく自律的にエラーに対処。
   * **結果**: 最終的に生成された `monitor.py` は、curses等に頼らずANSIエスケープと文字列操作だけで美しいアスキーアートグラフをリアルタイム描画する完璧な動作を見せました。
3. **ローカルLLMにおける「持久戦」パラダイム**
   * 商用クラウドAPI（Gemini等）であれば、この規模（2.7万トークン×数百ターン）の試行錯誤はAPIコストが数千円〜数万円単位に達しますが、ローカル環境（APU）では電気代のみで完遂可能であることが証明されました。生成速度が遅くとも（約1.5 token/s）、エラーに折れることなく無限に自律修正を続けられる「時間とコストのトレードオフ」の真価が発揮されました。

## 8. APU (UMA) 環境における長文コンテキスト処理の罠と対策 (2026/05/29 追記)

ローカルLLMをAPU（内蔵GPU）で稼働させ、エージェント用途（コンテキスト長が2万トークンを超えるようなユースケース）で運用する場合、特有のハードウェアクラッシュ問題が発生します。

*   **現象**: 
    コンテキストが 19,000 トークン付近に到達した際、突然 `vk::DeviceLostError`（Vulkan Device Lost）が発生し、`llama-server` がクラッシュ（コアダンプ）する。
*   **原因**: 
    APU環境でVulkanへのオフロード（`-ngl 10` 等）を設定し、かつ `--ubatch-size 1024` など大きなバッチサイズを指定していると発生する。巨大なKVキャッシュと大きなバッチサイズが組み合わさることで、Vulkanのコンピュートシェーダーの1回の処理時間がOSのGPUドライバのタイムアウトリミット（TDR）を超過してしまい、Linuxカーネルから強制リセット（Device Lost）を受けるため。
*   **対策**:
    `--ubatch-size 512`（またはそれ以下）に細分化する。
    バッチサイズを半分に落とすことで、1回あたりのVulkan計算時間（ディスパッチ時間）をタイムアウトリミット未満に抑え、長文コンテキストでもクラッシュせずに完走させることが可能になります。
*   **トレードオフ**:
    プロンプトの読み込み速度（Prompt Processing）は若干低下する（例: 17.5 t/s → 13 t/s 等）が、コード生成（Token Generation）の速度には影響しません。エージェント用途では「速度よりも、コンテキスト上限まで絶対にクラッシュしない堅牢性」が圧倒的に重要であるため、この設定が必須です。

## 9. プロキシを用いた強制 Temperature 設定による精度と速度の向上

エージェント（Kilo Code等）の挙動を安定させるため、間に挟んだプロキシ（`kilo_proxy.py`）でLLMへのリクエストを傍受・書き換える手法が極めて有効です。

*   **課題**: エディタ側の設定では Temperature を自由に指定できないケースがあり、デフォルトの `0.0` （あるいは `0.2` 等）で固定されてしまうことがある。
*   **対策**: プロキシ側でペイロード内の `temperature` パラメータを強制的に `0.6` に上書きして `llama-server` に転送する。
*   **驚くべき効果（MTPとの相乗効果）**: 
    Temperature を 0.6 に引き上げた結果、無限ループの抑止効果が得られただけでなく、出力されるコードが「より標準的で予測しやすい（自然な）」記述となり、**MTP（投機的デコード）のドラフト的中率（Draft Acceptance）が通常の 50% 程度から「76%〜82%」へと劇的に向上**しました。これにより、推論速度自体が大きく底上げされるという予期せぬメリットが確認されました。

## 10. 「Architectモデル」による自己レビューとコンテキスト要約の可能性（2026/05/29 追記）

今回のテストで特筆すべきは、Architectモデル（Qwen3.6-27B）が **「一度もコードを実行していない段階で、自ら出力した1万文字のコードを静的解析し、Python特有の破壊的代入エラー（文字列のイミュータビリティ等）を先回りして発見・修正した」** という事実です。2万トークンを超える文脈を維持したまま、この精度で自己レビューできる能力は非常に希少です。

*   **コンテキスト圧縮・他AIへの引き継ぎ（Handoff）**:
    この驚異的なコード理解力と要約能力は、「他の軽量・高速なAI（例: Qwen2.5-14B などのコーディング特化モデル）へタスクを引き継ぐためのインプット資料（Handoff Document）作成」に極めて適しています。
*   **マルチエージェント的運用イメージ**:
    2万トークン以上の膨大なやり取り（仕様変更や試行錯誤の歴史）をArchitectモデルに読み込ませ、「現在のアーキテクチャの課題」「未解決のバグ」「次に実装すべき機能」を簡潔なMarkdownに要約させます。これを次のプロンプトの初期コンテキストとして別モデルに渡すことで、安価かつ高速なモデルでも文脈を失わずに正確なコーディングを継続できる、という強力なパイプラインが構築可能です。

## 11. チャットUIからの脱却と「バッチ処理（パイプライン）型」アーキテクチャの必要性（2026/05/29 追記）

現在主流となっている「AIと人間が対話しながら開発を進める（チャット型UI）」というパラダイム自体が、実は自律型エージェントのパフォーマンスを阻害している可能性があります。

*   **チャット型UIの限界**:
    チャット形式は「過去の全てのやり取り」をシーケンシャル（直線的）にコンテキストに積み上げていく構造になっているため、必然的に記憶の肥大化とAttentionの希薄化（要件の忘却）を引き起こします。
*   **バッチ処理的フロー（ステートマシン）への移行**:
    本来、高度なソフトウェア開発はチャットではなく「バッチ処理（パイプライン）」として設計されるべきです。
    1. **設計ノード (Architect)**: 要求を入力し、`design.md` と `task.md` を出力（完了後にプロセス終了）。
    2. **実装ノード (Coder)**: `task.md` を入力とし、ソースコードを出力（完了後にプロセス終了）。
    3. **検証ノード (Tester)**: ソースコードを入力とし、テスト結果を出力。エラーがあれば「エラーログ」のみを新しい入力として実装ノードを再実行。
*   **Antigravity SDK / LangGraph の思想**:
    このように「状態（ステート）と成果物（ファイル）」だけをノード間でバケツリレーのように渡していくアーキテクチャであれば、コンテキストは常に最小限（最新の成果物のみ）に保たれます。ローカルLLMにおける究極のエージェント環境は、チャットUIに囚われない「非同期バッチ処理型のマルチエージェント・パイプライン」の構築にあります。

## 12. マルチモデル（マルチエージェント）の並列稼働とプロキシのフォールバックアーキテクチャ (2026/05/30 追記)

ローカルAPU環境（例: 48GB統合メモリ）を活用し、「重い/賢いモデル（27B）」と「軽い/速いモデル（14B/7B）」を2つ並列に常時稼働させるアーキテクチャが実現可能であることが実証されました。

*   **動的ルーティングとフェイルセーフ**:
    プロキシサーバー（`kilo_proxy.py`）に以下のロジックを実装することで、単一のエディタ環境からロールを切り替えるだけで、用途に応じたモデルに自動転送されるパイプラインが完成しました。
    *   **デフォルト (Port 9090)**: Architect/Code等のロールは27Bモデルへ。
    *   **デバッグ用 (Port 9091)**: `Test Engineer` や `Debug` のロールは14B/7Bへ。
    *   **強力なフォールバック機構**: もしPort 9091のプロセスが落ちている場合（未起動時）でも、プロキシがポートの死活監視を瞬時に行い、自動的に「Port 9090」へ転送（フォールバック）します。これによりユーザーへのエラー露出を100%防ぎます。

*   **メモリサイジングとKVキャッシュ圧縮（4-bit）**:
    27Bモデルと14Bモデルを同時に動かすとメモリ枯渇（OOM）が懸念されますが、起動時に `-ctk q4_0 -ctv q4_0` を指定してKVキャッシュを4-bit量子化することで、64,000トークンの巨大コンテキストでもキャッシュメモリを1モデルあたり2GB〜4GB程度に抑えられます。結果、48GB環境ならOS分を含めても15GB以上の空きメモリが残る、極めて余裕のある動作が可能です。

*   **モデル毎のバッチサイズチューニング（帯域対策）**:
    27BモデルはAPUの帯域を占有するため、GPUドライバのTDR（クラッシュ）を避けるために `--ubatch-size 128` までの制限が必要でした。しかし、14Bモデルは帯域への負荷が圧倒的に低いため、**`--ubatch-size 512`** に引き上げて起動することが可能です。これにより、長文の読み込み速度（Prompt Evaluation）が劇的に改善します。

## 13. IDE・エージェント連携におけるプロキシタイムアウトの落とし穴 (2026/05/30 追記)

VS CodeやエディタのAIエージェント（Kilo Code等）を使っていて、「数秒で突然レスポンスが空で返ってくる」「生成がすぐに止まってしまう」という現象に遭遇した場合、それはLLMのサボりではなく**プロキシサーバー等のタイムアウト設定**が原因である可能性が高いです。

*   **現象**: 
    巨大なコンテキストを読み込んでいる際（Prompt Evaluation）、AIが最初の1文字（First Token）を出力するまでに数分〜数十分かかることがあります。この間、プロキシ（例: FlaskやQueue）が「応答がない」と判定し、デフォルトの5秒等でタイムアウトして空のレスポンス（HTTP 200）をエディタに返してしまう。
*   **対策**:
    プロキシサーバー内でキューやリクエストを待機する時間（timeoutパラメータ）を、LLMの最大推論時間に合わせて **`timeout=600.0` （10分）など極端に長く設定** する必要があります。これにより、APUのような持久戦型のローカルLLMでも、途切れることなく確実にエージェントに結果を返すことができます。

---
*Created on: 2026-05-27, Updated on: 2026-05-30*
